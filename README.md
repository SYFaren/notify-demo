# Notify

Minimal demo: type a message → system notification.

Live: https://syfaren.github.io/notify-demo/

## Run locally

```bash
python3 -m http.server 5173
```

Open http://localhost:5173

Notifications need a secure context (`localhost` or HTTPS).
